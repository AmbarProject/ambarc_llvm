func main() -> int {
  iterations: int = 100000000;
  pi: float = 0.0;
  sign: float = 1.0;
  
  for(i: int =0; i < iterations; i = i + 1) {
    pi = pi + sign / (2.9 * i + 1.0);
    sign = -sign;

  }
}
